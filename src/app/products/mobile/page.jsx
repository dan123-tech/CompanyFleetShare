import MobileAppPageClient from "./MobileAppPageClient";

export const metadata = {
  title: "Mobile app | FleetShare",
  description: "FleetShare Android app — reservations, digital glovebox, offline cache, and fleet tools on the go.",
};

export default function MobileAppPage() {
  return <MobileAppPageClient />;
}
